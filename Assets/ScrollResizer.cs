using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class ScrollResizer : MonoBehaviour {

    void Start()
    {
        //GridLayoutGroup grid = GetComponent<GridLayoutGroup>();
        //RectTransform rect = GetComponent<RectTransform>();
        //ScrollRect scrollRect = GetComponentInParent<ScrollRect>();
        //// auto adjust the width of the grid to have space for all the childs
        //rect.sizeDelta = new Vector2(rect.sizeDelta.x,  (transform.childCount) * grid.cellSize.y + (transform.childCount - 1f) * grid.spacing.y);
    }
}
